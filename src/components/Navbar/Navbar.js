import React from 'react';

const Navbar = () => {
  return (
    <nav data-testid='nav'>
      <button>Home</button>
      <button>Sign in</button>
      <button>Sign up</button>
    </nav>
  );
};

export default Navbar;
